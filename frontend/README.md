# Setup

1. Init configuration
    ```bash
    ./cmd init
    ```

2. Build docker images
    ```bash
    ./cmd docker build
    ```
    
3. Start docker services
    ```bash
    ./cmd docker up
    ```